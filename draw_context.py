class DrawContext:
    def __init__(self, screen, font, frame, total_frames, width, height):
        self.screen = screen
        self.font = font
        self.frame = frame
        self.total_frames = total_frames
        self.width = width
        self.height = height